/**
 * Print queue schema — PostgreSQL + Prisma bilan boshqariladi.
 * Barcha jadvallar prisma/schema.prisma orqali avtomatik yaratiladi.
 * Bu funksiya eski SQLite (pos.db) versiyasi bilan moslashish uchun saqlanib qoldi
 * lekin hozir hech narsa bajarmaydigan stub ga aylantirildi.
 */

export async function ensurePrintSchema(): Promise<void> {
    // PostgreSQL + Prisma Migrate boshqaradi — hech qanday qo'shimcha amal talab etilmaydi
    return;
}
